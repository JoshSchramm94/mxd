% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betas_post.R
\name{betas_post}
\alias{betas_post}
\title{Preparation of posterior individuals draws}
\usage{
betas_post(stan_output, bw_size, cores = 1L, labels = NULL, anchor = FALSE)
}
\arguments{
\item{stan_output}{stanfit object}

\item{bw_size}{size of MaxDiff tasks in study}

\item{cores}{optional integer input to define the number of cores used for
calculation (default set to 1L)}

\item{labels}{optional character vector to define labels of items}

\item{anchor}{logical vector to indicate whether it is an anchored MaxDiff}
}
\value{
a list with 3 objects
\describe{
\item{beta_raw}{raw individual utitlies}
\item{beta_zc}{zero-centered individual utilities}
\item{beta_prob}{probability scores of the individuals}
}
}
\description{
Function to prepare the posterior distribution from the individual draws
(i.e., \code{beta} draws).
}
\details{
\code{betas_post()} prepares the posterior distribution for the individual (i.e.,
\code{betas}). Users have to provide the output of the stan model (e.g.,
estimated using the \code{mxd_hb()}) function. Since the utilities from the
posterior distribution are also transformed into choice probabilities, users
have to specify the number of items shown per MaxDiff task (i.e., \code{bw_size}).
Similiarily, if an anchored MaxDiff was applied (default set to \code{FALSE}),
this has to be specified in the \code{anchor} argument. To speed up the
calculation, multiple cores can be used (\code{cores}). To determine how many
cores are available users can use, for example, the
\code{\link[parallelly]{availableCores}} function. To match the utilities
with the original ids, specify a vector with actual ids in \code{ids}.
}
\examples{
\dontrun{
betas_prep <- betas_post(
  stan_output = mxd_model,
  bw_size = 4,
  cores = 4L,
  labels = paste0("v", seq_len(16)),
  anchor = TRUE
)
}

}
