% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dm_to_stan_hb.R
\name{dm_to_stan_hb}
\alias{dm_to_stan_hb}
\title{Prepare design matrix for Hierarchical Bayes estimation in Stan}
\usage{
dm_to_stan_hb(
  design,
  id,
  cs,
  alt,
  items,
  ch,
  type,
  anchor_start = NULL,
  prior_b = NULL,
  prior_omega = NULL,
  prior_sigma = NULL,
  demos = NULL
)
}
\arguments{
\item{design}{design matrix}

\item{id}{column name of the id variable}

\item{cs}{column name of the choice set variable}

\item{alt}{column name of the variable marking alternatives within choice
sets}

\item{items}{column names of the predictor variables}

\item{ch}{column name of the choice variable}

\item{type}{character to specify coding method}

\item{anchor_start}{numeric input to specify the starting cs for the anchor
questions if \code{type = "maxdiff"}. If unanchored and \code{type = "maxdiff"} or
different \code{type} specified, leave empty}

\item{prior_b}{numeric input for the b prior}

\item{prior_omega}{numeric input for the omega prior}

\item{prior_sigma}{numeric input for the sigma prior}

\item{demos}{matrix of demographic variables (i.e., Z variables)}
}
\value{
a named list with stan input that is required for the model of \code{mxd_hb()}
}
\description{
Function to convert the design matrix to input required for \code{mxd_hb()}.
}
\details{
\code{dm_to_stan_hb()} converts the design matrix into a nested list. The input
is required to run the hierarchical Bayes estimation using \code{mxd_hb()}.
Users have to define the design matrix (\code{design}), the variables for the
participants identifier (\code{id}), the choice set (\code{cs}), the alternative
within the choice set (\code{alt}), the items (i.e., predictors; \code{items}) and the
actual choice variable (\code{ch}). For \code{type}, please specify the type
of coding assumed (see also \code{\link[mxd]{csv_to_dm}}).
Further, the use can specify the priors for the hyperparameters \code{b},
\code{omega}, and \code{sigma}.

\describe{
\item{prior_b}{prior for the population mean (mean of hyperparameter) of
the utilities; default is set to \code{5}}
\item{prior_omega}{prior for the LKJ cholesky of the correlation matrix;
default is set to \code{2}}
\item{prior_sigma}{prior for the scale parameter of the utilities;
default is set to \code{5}}
}

In addition, \emph{Z} variables can be defined, i.e., demographic variables. The
intercept for \code{demos} will be added in the function.
}
