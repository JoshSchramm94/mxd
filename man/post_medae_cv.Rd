% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/post_medae_cv.R
\name{post_medae_cv}
\alias{post_medae_cv}
\title{Cross-fold median absolute error}
\usage{
post_medae_cv(
  stan_cv,
  stan_input,
  hot_data,
  opts,
  hot_choice,
  val_id,
  hot_id,
  labels = NULL,
  raw = FALSE
)
}
\arguments{
\item{stan_cv}{cross-fold stan object}

\item{stan_input}{cross-fold stan input}

\item{hot_data}{data frame with actual hot choice}

\item{opts}{variable names of items}

\item{hot_choice}{variable name of actual choice}

\item{val_id}{variable name of validation id}

\item{hot_id}{variable name of id in actual data frame}

\item{labels}{optional character vector to define labels of predictors}

\item{raw}{logical vector to indicate whether raw or aggregated results
should be reported}
}
\value{
a tibble
}
\description{
Function to calculate posterior median absolute error for each k-fold.
}
\details{
\code{post_medae_cv()} calculates the posterior media absolute error (MedAE) of a
validation task for a k-fold validation sample. \code{stan_cv} should be the
output of a hierarchical Bayesian k-fold cross-validation estimation
(see \code{\link[mxd]{mxd_hb_cv}}). \code{stan_input} needs to be the input that
was required for \code{mxd_hb_cv()}.
\code{hot_data} must be a data frame with the participants' actual choice in the
validation task (\code{hot_choice}). The respondents in \code{hot_data} do not have to
be the same as in \code{betas_post} that means it could also be a validation sample.
The options in the validation task are specified in the \code{opts} argument
(make sure that they have the same order as the variables shown in the
validation task). \code{hot_choice} must be the column name of the actual choice
in the validation task in \code{hot_data}. Finally, users can
decide whether they want the \code{raw} results (set \code{raw} to \code{TRUE}) to get the
MedAE for each posterior draw for each k-fold or if the output should be
aggregated across folds (i.e., set \code{raw} to \code{FALSE}).
}
