% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mxd_count.R
\name{mxd_count}
\alias{mxd_count}
\title{MaxDiff Count Analysis}
\usage{
mxd_count(design, cs, item, ch, no_items, group = NULL, labels = NULL)
}
\arguments{
\item{design}{unanchored csv design}

\item{cs}{column name of the choice set variable}

\item{item}{column name of the item variable}

\item{ch}{column name of the choice variable}

\item{no_items}{numeric input to specify number of MaxDiff tasks}

\item{group}{optional column name to display results by grouping variable(s)}

\item{labels}{optional character vector to define labels of items}
}
\value{
a tibble
}
\description{
Function to run count analysis to get best and worst counts as well as
difference between best and worst (bw scores).
}
\details{
Input has to be the \strong{unanchored} csv export from Lighthouse Studio that
also includes the participants' choices. Users have to specify the
(\code{design}), the variable indicating the choice set (\code{cs}), the column that
indicates which item was shown (\code{item}), the choice column (\code{ch}), the total
number of items included (\code{no_items}). Further, the user can define an
optional grouping variable (\code{group}) as well as labels for the items
(\code{labels}).
}
